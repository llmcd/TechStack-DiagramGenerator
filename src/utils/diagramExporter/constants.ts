export const EXPORT_DEFAULTS = {
  width: 800,
  padding: 20,
  headerWidth: 250,
  rowHeight: 80,
  titleHeight: 60,
  itemHeight: 60,
  cornerRadius: 8,
  logoSize: 24,
  fontSize: {
    title: 24,
    header: 16,
    content: 14
  }
} as const;

export const FONTS = {
  primary: 'Arial',
  fallback: 'sans-serif'
} as const;